{
  "name": "team-task-manager-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Workspace tooling for the Team Task Manager full-stack application.",
  "scripts": {
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "test": "npm --prefix backend test && npm --prefix frontend test",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
